# Production Environment Configuration
# Copy this to .env.production for production builds: cp .env.production.example .env.production
# DO NOT commit .env.production to git (contains production URLs)

# Backend API URL (Railway production)
VITE_BACKEND_URL=https://your-app.railway.app

# Note: Replace 'your-app.railway.app' with your actual Railway domain
# You can find this in Railway dashboard after deployment
