[pytest]
testpaths = tests
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (deselected in CI with -m "not slow")
    integration: requires real network access (deselected in CI)
